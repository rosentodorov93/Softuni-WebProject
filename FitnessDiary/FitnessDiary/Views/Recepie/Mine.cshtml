@model IEnumerable<RecipeListingViewModel>

@{
    ViewBag.Title = "My Recipes";
}

<h1>@ViewBag.Title</h1>

<div class="row">
    @if (Model.Any())
    {
        foreach (var recipe in Model)
        {
            <div class="card card-primary">
                <div class="card-header">
                    <h3 class="card-title">@recipe.Name</h3>
                    <!-- /.card-tools -->
                </div>
                <!-- /.card-header -->
                <div class="card-body">
                    <p>Servings @recipe.ServingsSize</p>
                    <p>Units @recipe.Unit</p>
                    <p>Total Calories @recipe.TotalCalories</p>
                    <p>Calorie per Serving @recipe.CaloriesPerPortion</p>
                    
                    <a asp-controller="Recepie" asp-action="AddIngredient" asp-route-id="@recipe.Id">Add more ingredients</a>
                    <a asp-controller="Recepie" asp-action="Details" asp-route-id="@recipe.Id">Add more ingredients</a>
                    <a asp-controller="Recepie" asp-action="FinishRecipie" asp-route-id="@recipe.Id">Finilize</a>
                </div>
                <!-- /.card-body -->
                <div class="card-footer">
                    The footer of the card
                </div>
                <!-- /.card-footer -->
            </div>
            <!-- /.card -->
        }
    }
</div>

